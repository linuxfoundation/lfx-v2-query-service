// Code generated by goa, DO NOT EDIT.
//
// lfx-v2-query-service HTTP client CLI support package
//
// Command:
// $ goa gen github.com/linuxfoundation/lfx-v2-query-service/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	querysvcc "github.com/linuxfoundation/lfx-v2-query-service/gen/http/query_svc/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"query-svc (query-resources|query-resources-count|query-orgs|suggest-orgs|readyz|livez)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "query-svc query-resources --version \"1\" --name \"gov board\" --parent \"project:123\" --type \"committee\" --tags '[\n      \"active\",\n      \"public\"\n   ]' --tags-all '[\n      \"governance\",\n      \"security\"\n   ]' --date-field \"updated_at\" --date-from \"2025-01-10\" --date-to \"2025-01-28\" --filters '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-all '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-or '[\n      \"mailing_list_id:abc\",\n      \"mailing_list_id:xyz\"\n   ]' --cel-filter \"data.slug == \\\"tlf\\\"\" --filter-grants \"direct\" --sort \"updated_desc\" --page-token \"****\" --page-size 20 --bearer-token \"eyJhbGci...\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		querySvcFlags = flag.NewFlagSet("query-svc", flag.ContinueOnError)

		querySvcQueryResourcesFlags            = flag.NewFlagSet("query-resources", flag.ExitOnError)
		querySvcQueryResourcesVersionFlag      = querySvcQueryResourcesFlags.String("version", "REQUIRED", "")
		querySvcQueryResourcesNameFlag         = querySvcQueryResourcesFlags.String("name", "", "")
		querySvcQueryResourcesParentFlag       = querySvcQueryResourcesFlags.String("parent", "", "")
		querySvcQueryResourcesTypeFlag         = querySvcQueryResourcesFlags.String("type", "", "")
		querySvcQueryResourcesTagsFlag         = querySvcQueryResourcesFlags.String("tags", "", "")
		querySvcQueryResourcesTagsAllFlag      = querySvcQueryResourcesFlags.String("tags-all", "", "")
		querySvcQueryResourcesDateFieldFlag    = querySvcQueryResourcesFlags.String("date-field", "", "")
		querySvcQueryResourcesDateFromFlag     = querySvcQueryResourcesFlags.String("date-from", "", "")
		querySvcQueryResourcesDateToFlag       = querySvcQueryResourcesFlags.String("date-to", "", "")
		querySvcQueryResourcesFiltersFlag      = querySvcQueryResourcesFlags.String("filters", "", "")
		querySvcQueryResourcesFiltersAllFlag   = querySvcQueryResourcesFlags.String("filters-all", "", "")
		querySvcQueryResourcesFiltersOrFlag    = querySvcQueryResourcesFlags.String("filters-or", "", "")
		querySvcQueryResourcesCelFilterFlag    = querySvcQueryResourcesFlags.String("cel-filter", "", "")
		querySvcQueryResourcesFilterGrantsFlag = querySvcQueryResourcesFlags.String("filter-grants", "", "")
		querySvcQueryResourcesSortFlag         = querySvcQueryResourcesFlags.String("sort", "name_asc", "")
		querySvcQueryResourcesPageTokenFlag    = querySvcQueryResourcesFlags.String("page-token", "", "")
		querySvcQueryResourcesPageSizeFlag     = querySvcQueryResourcesFlags.String("page-size", "50", "")
		querySvcQueryResourcesBearerTokenFlag  = querySvcQueryResourcesFlags.String("bearer-token", "REQUIRED", "")

		querySvcQueryResourcesCountFlags           = flag.NewFlagSet("query-resources-count", flag.ExitOnError)
		querySvcQueryResourcesCountVersionFlag     = querySvcQueryResourcesCountFlags.String("version", "REQUIRED", "")
		querySvcQueryResourcesCountNameFlag        = querySvcQueryResourcesCountFlags.String("name", "", "")
		querySvcQueryResourcesCountParentFlag      = querySvcQueryResourcesCountFlags.String("parent", "", "")
		querySvcQueryResourcesCountTypeFlag        = querySvcQueryResourcesCountFlags.String("type", "", "")
		querySvcQueryResourcesCountTagsFlag        = querySvcQueryResourcesCountFlags.String("tags", "", "")
		querySvcQueryResourcesCountTagsAllFlag     = querySvcQueryResourcesCountFlags.String("tags-all", "", "")
		querySvcQueryResourcesCountDateFieldFlag   = querySvcQueryResourcesCountFlags.String("date-field", "", "")
		querySvcQueryResourcesCountDateFromFlag    = querySvcQueryResourcesCountFlags.String("date-from", "", "")
		querySvcQueryResourcesCountDateToFlag      = querySvcQueryResourcesCountFlags.String("date-to", "", "")
		querySvcQueryResourcesCountFiltersFlag     = querySvcQueryResourcesCountFlags.String("filters", "", "")
		querySvcQueryResourcesCountFiltersAllFlag  = querySvcQueryResourcesCountFlags.String("filters-all", "", "")
		querySvcQueryResourcesCountFiltersOrFlag   = querySvcQueryResourcesCountFlags.String("filters-or", "", "")
		querySvcQueryResourcesCountGroupByFlag     = querySvcQueryResourcesCountFlags.String("group-by", "", "")
		querySvcQueryResourcesCountGroupBySizeFlag = querySvcQueryResourcesCountFlags.String("group-by-size", "", "")
		querySvcQueryResourcesCountMetricFlag      = querySvcQueryResourcesCountFlags.String("metric", "", "")
		querySvcQueryResourcesCountBearerTokenFlag = querySvcQueryResourcesCountFlags.String("bearer-token", "REQUIRED", "")

		querySvcQueryOrgsFlags           = flag.NewFlagSet("query-orgs", flag.ExitOnError)
		querySvcQueryOrgsVersionFlag     = querySvcQueryOrgsFlags.String("version", "REQUIRED", "")
		querySvcQueryOrgsNameFlag        = querySvcQueryOrgsFlags.String("name", "", "")
		querySvcQueryOrgsDomainFlag      = querySvcQueryOrgsFlags.String("domain", "", "")
		querySvcQueryOrgsBearerTokenFlag = querySvcQueryOrgsFlags.String("bearer-token", "REQUIRED", "")

		querySvcSuggestOrgsFlags           = flag.NewFlagSet("suggest-orgs", flag.ExitOnError)
		querySvcSuggestOrgsVersionFlag     = querySvcSuggestOrgsFlags.String("version", "REQUIRED", "")
		querySvcSuggestOrgsQueryFlag       = querySvcSuggestOrgsFlags.String("query", "REQUIRED", "")
		querySvcSuggestOrgsBearerTokenFlag = querySvcSuggestOrgsFlags.String("bearer-token", "REQUIRED", "")

		querySvcReadyzFlags = flag.NewFlagSet("readyz", flag.ExitOnError)

		querySvcLivezFlags = flag.NewFlagSet("livez", flag.ExitOnError)
	)
	querySvcFlags.Usage = querySvcUsage
	querySvcQueryResourcesFlags.Usage = querySvcQueryResourcesUsage
	querySvcQueryResourcesCountFlags.Usage = querySvcQueryResourcesCountUsage
	querySvcQueryOrgsFlags.Usage = querySvcQueryOrgsUsage
	querySvcSuggestOrgsFlags.Usage = querySvcSuggestOrgsUsage
	querySvcReadyzFlags.Usage = querySvcReadyzUsage
	querySvcLivezFlags.Usage = querySvcLivezUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "query-svc":
			svcf = querySvcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "query-svc":
			switch epn {
			case "query-resources":
				epf = querySvcQueryResourcesFlags

			case "query-resources-count":
				epf = querySvcQueryResourcesCountFlags

			case "query-orgs":
				epf = querySvcQueryOrgsFlags

			case "suggest-orgs":
				epf = querySvcSuggestOrgsFlags

			case "readyz":
				epf = querySvcReadyzFlags

			case "livez":
				epf = querySvcLivezFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "query-svc":
			c := querysvcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "query-resources":
				endpoint = c.QueryResources()
				data, err = querysvcc.BuildQueryResourcesPayload(*querySvcQueryResourcesVersionFlag, *querySvcQueryResourcesNameFlag, *querySvcQueryResourcesParentFlag, *querySvcQueryResourcesTypeFlag, *querySvcQueryResourcesTagsFlag, *querySvcQueryResourcesTagsAllFlag, *querySvcQueryResourcesDateFieldFlag, *querySvcQueryResourcesDateFromFlag, *querySvcQueryResourcesDateToFlag, *querySvcQueryResourcesFiltersFlag, *querySvcQueryResourcesFiltersAllFlag, *querySvcQueryResourcesFiltersOrFlag, *querySvcQueryResourcesCelFilterFlag, *querySvcQueryResourcesFilterGrantsFlag, *querySvcQueryResourcesSortFlag, *querySvcQueryResourcesPageTokenFlag, *querySvcQueryResourcesPageSizeFlag, *querySvcQueryResourcesBearerTokenFlag)
			case "query-resources-count":
				endpoint = c.QueryResourcesCount()
				data, err = querysvcc.BuildQueryResourcesCountPayload(*querySvcQueryResourcesCountVersionFlag, *querySvcQueryResourcesCountNameFlag, *querySvcQueryResourcesCountParentFlag, *querySvcQueryResourcesCountTypeFlag, *querySvcQueryResourcesCountTagsFlag, *querySvcQueryResourcesCountTagsAllFlag, *querySvcQueryResourcesCountDateFieldFlag, *querySvcQueryResourcesCountDateFromFlag, *querySvcQueryResourcesCountDateToFlag, *querySvcQueryResourcesCountFiltersFlag, *querySvcQueryResourcesCountFiltersAllFlag, *querySvcQueryResourcesCountFiltersOrFlag, *querySvcQueryResourcesCountGroupByFlag, *querySvcQueryResourcesCountGroupBySizeFlag, *querySvcQueryResourcesCountMetricFlag, *querySvcQueryResourcesCountBearerTokenFlag)
			case "query-orgs":
				endpoint = c.QueryOrgs()
				data, err = querysvcc.BuildQueryOrgsPayload(*querySvcQueryOrgsVersionFlag, *querySvcQueryOrgsNameFlag, *querySvcQueryOrgsDomainFlag, *querySvcQueryOrgsBearerTokenFlag)
			case "suggest-orgs":
				endpoint = c.SuggestOrgs()
				data, err = querysvcc.BuildSuggestOrgsPayload(*querySvcSuggestOrgsVersionFlag, *querySvcSuggestOrgsQueryFlag, *querySvcSuggestOrgsBearerTokenFlag)
			case "readyz":
				endpoint = c.Readyz()
			case "livez":
				endpoint = c.Livez()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// querySvcUsage displays the usage of the query-svc command and its
// subcommands.
func querySvcUsage() {
	fmt.Fprintln(os.Stderr, `The query service provides resource and user queries.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] query-svc COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    query-resources: Locate resources by their type or parent, or use typeahead search to query resources by a display name or similar alias.`)
	fmt.Fprintln(os.Stderr, `    query-resources-count: Count matching resources by query.`)
	fmt.Fprintln(os.Stderr, `    query-orgs: Locate a single organization by name or domain.`)
	fmt.Fprintln(os.Stderr, `    suggest-orgs: Get organization suggestions for typeahead search based on a query.`)
	fmt.Fprintln(os.Stderr, `    readyz: Check if the service is able to take inbound requests.`)
	fmt.Fprintln(os.Stderr, `    livez: Check if the service is alive.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s query-svc COMMAND --help\n", os.Args[0])
}
func querySvcQueryResourcesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc query-resources", os.Args[0])
	fmt.Fprint(os.Stderr, " -version STRING")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -parent STRING")
	fmt.Fprint(os.Stderr, " -type STRING")
	fmt.Fprint(os.Stderr, " -tags JSON")
	fmt.Fprint(os.Stderr, " -tags-all JSON")
	fmt.Fprint(os.Stderr, " -date-field STRING")
	fmt.Fprint(os.Stderr, " -date-from STRING")
	fmt.Fprint(os.Stderr, " -date-to STRING")
	fmt.Fprint(os.Stderr, " -filters JSON")
	fmt.Fprint(os.Stderr, " -filters-all JSON")
	fmt.Fprint(os.Stderr, " -filters-or JSON")
	fmt.Fprint(os.Stderr, " -cel-filter STRING")
	fmt.Fprint(os.Stderr, " -filter-grants STRING")
	fmt.Fprint(os.Stderr, " -sort STRING")
	fmt.Fprint(os.Stderr, " -page-token STRING")
	fmt.Fprint(os.Stderr, " -page-size INT")
	fmt.Fprint(os.Stderr, " -bearer-token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Locate resources by their type or parent, or use typeahead search to query resources by a display name or similar alias.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -version STRING: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -parent STRING: `)
	fmt.Fprintln(os.Stderr, `    -type STRING: `)
	fmt.Fprintln(os.Stderr, `    -tags JSON: `)
	fmt.Fprintln(os.Stderr, `    -tags-all JSON: `)
	fmt.Fprintln(os.Stderr, `    -date-field STRING: `)
	fmt.Fprintln(os.Stderr, `    -date-from STRING: `)
	fmt.Fprintln(os.Stderr, `    -date-to STRING: `)
	fmt.Fprintln(os.Stderr, `    -filters JSON: `)
	fmt.Fprintln(os.Stderr, `    -filters-all JSON: `)
	fmt.Fprintln(os.Stderr, `    -filters-or JSON: `)
	fmt.Fprintln(os.Stderr, `    -cel-filter STRING: `)
	fmt.Fprintln(os.Stderr, `    -filter-grants STRING: `)
	fmt.Fprintln(os.Stderr, `    -sort STRING: `)
	fmt.Fprintln(os.Stderr, `    -page-token STRING: `)
	fmt.Fprintln(os.Stderr, `    -page-size INT: `)
	fmt.Fprintln(os.Stderr, `    -bearer-token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc query-resources --version \"1\" --name \"gov board\" --parent \"project:123\" --type \"committee\" --tags '[\n      \"active\",\n      \"public\"\n   ]' --tags-all '[\n      \"governance\",\n      \"security\"\n   ]' --date-field \"updated_at\" --date-from \"2025-01-10\" --date-to \"2025-01-28\" --filters '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-all '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-or '[\n      \"mailing_list_id:abc\",\n      \"mailing_list_id:xyz\"\n   ]' --cel-filter \"data.slug == \\\"tlf\\\"\" --filter-grants \"direct\" --sort \"updated_desc\" --page-token \"****\" --page-size 20 --bearer-token \"eyJhbGci...\"")
}

func querySvcQueryResourcesCountUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc query-resources-count", os.Args[0])
	fmt.Fprint(os.Stderr, " -version STRING")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -parent STRING")
	fmt.Fprint(os.Stderr, " -type STRING")
	fmt.Fprint(os.Stderr, " -tags JSON")
	fmt.Fprint(os.Stderr, " -tags-all JSON")
	fmt.Fprint(os.Stderr, " -date-field STRING")
	fmt.Fprint(os.Stderr, " -date-from STRING")
	fmt.Fprint(os.Stderr, " -date-to STRING")
	fmt.Fprint(os.Stderr, " -filters JSON")
	fmt.Fprint(os.Stderr, " -filters-all JSON")
	fmt.Fprint(os.Stderr, " -filters-or JSON")
	fmt.Fprint(os.Stderr, " -group-by STRING")
	fmt.Fprint(os.Stderr, " -group-by-size INT")
	fmt.Fprint(os.Stderr, " -metric STRING")
	fmt.Fprint(os.Stderr, " -bearer-token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Count matching resources by query.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -version STRING: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -parent STRING: `)
	fmt.Fprintln(os.Stderr, `    -type STRING: `)
	fmt.Fprintln(os.Stderr, `    -tags JSON: `)
	fmt.Fprintln(os.Stderr, `    -tags-all JSON: `)
	fmt.Fprintln(os.Stderr, `    -date-field STRING: `)
	fmt.Fprintln(os.Stderr, `    -date-from STRING: `)
	fmt.Fprintln(os.Stderr, `    -date-to STRING: `)
	fmt.Fprintln(os.Stderr, `    -filters JSON: `)
	fmt.Fprintln(os.Stderr, `    -filters-all JSON: `)
	fmt.Fprintln(os.Stderr, `    -filters-or JSON: `)
	fmt.Fprintln(os.Stderr, `    -group-by STRING: `)
	fmt.Fprintln(os.Stderr, `    -group-by-size INT: `)
	fmt.Fprintln(os.Stderr, `    -metric STRING: `)
	fmt.Fprintln(os.Stderr, `    -bearer-token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc query-resources-count --version \"1\" --name \"gov board\" --parent \"project:123\" --type \"committee\" --tags '[\n      \"active\",\n      \"public\"\n   ]' --tags-all '[\n      \"governance\",\n      \"security\"\n   ]' --date-field \"updated_at\" --date-from \"2025-01-10\" --date-to \"2025-01-28\" --filters '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-all '[\n      \"status:active\",\n      \"priority:high\"\n   ]' --filters-or '[\n      \"mailing_list_id:abc\",\n      \"mailing_list_id:xyz\"\n   ]' --group-by \"project_uid\" --group-by-size 100 --metric \"\" --bearer-token \"eyJhbGci...\"")
}

func querySvcQueryOrgsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc query-orgs", os.Args[0])
	fmt.Fprint(os.Stderr, " -version STRING")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -domain STRING")
	fmt.Fprint(os.Stderr, " -bearer-token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Locate a single organization by name or domain.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -version STRING: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: `)
	fmt.Fprintln(os.Stderr, `    -domain STRING: `)
	fmt.Fprintln(os.Stderr, `    -bearer-token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc query-orgs --version \"1\" --name \"The Linux Foundation\" --domain \"linuxfoundation.org\" --bearer-token \"eyJhbGci...\"")
}

func querySvcSuggestOrgsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc suggest-orgs", os.Args[0])
	fmt.Fprint(os.Stderr, " -version STRING")
	fmt.Fprint(os.Stderr, " -query STRING")
	fmt.Fprint(os.Stderr, " -bearer-token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get organization suggestions for typeahead search based on a query.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -version STRING: `)
	fmt.Fprintln(os.Stderr, `    -query STRING: `)
	fmt.Fprintln(os.Stderr, `    -bearer-token STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc suggest-orgs --version \"1\" --query \"linux\" --bearer-token \"eyJhbGci...\"")
}

func querySvcReadyzUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc readyz", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is able to take inbound requests.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc readyz")
}

func querySvcLivezUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] query-svc livez", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Check if the service is alive.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "query-svc livez")
}
